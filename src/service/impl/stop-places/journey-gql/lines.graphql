query getHarbors($authorities: [String]) {
  lines(authorities: $authorities, transportModes: water) {
    quays {
      stopPlace {
        name
        id
        latitude
        longitude
      }
    }
    transportSubmode
  }
}
