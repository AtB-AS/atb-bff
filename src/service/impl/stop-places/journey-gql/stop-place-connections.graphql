query getStopPlaceConnections($id: String!) {
  stopPlace(id: $id) {
    quays {
      journeyPatterns {
        quays {
          stopPlace {
            ...stopPlace
          }
        }
      }
    }
  }
}
