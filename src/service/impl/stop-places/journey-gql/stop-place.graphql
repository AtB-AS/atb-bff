query getStopPlace($id: String!) {
  stopPlace(id: $id) {
    quays {
      journeyPatterns {
        quays {
          stopPlace {
            name
            id
          }
        }
      }
    }
  }
}
